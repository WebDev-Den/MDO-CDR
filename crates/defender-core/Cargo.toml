[package]
name = "defender-core"
version = "0.1.0"
edition = "2024"
description = "Core policy schema and shared contracts for the mdo_cdr workspace"
documentation = "https://docs.rs/defender-core"
license = "MIT"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"
